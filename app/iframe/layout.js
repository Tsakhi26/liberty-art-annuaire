import "../globals.css"

export const metadata = {
  title: "Liberty Art - Galerie",
  description: "Galerie des artistes Liberty Art",
}

export default function IframeLayout({ children }) {
  return children
}
